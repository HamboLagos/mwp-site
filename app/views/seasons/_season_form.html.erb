<%= form_for @season do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :year %>
  <%= f.text_field :year, placeholder: "#{Time.now.year}" %>

  <%= f.submit "Submit", class: 'btn btn-large btn-primary' %>
<% end %>
